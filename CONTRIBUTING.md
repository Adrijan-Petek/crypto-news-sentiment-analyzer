# Contributing

## Development setup

```bash
npm install
cp .env.example .env
```

## Checks

```bash
npm test
npm run lint
npm run format:check
```

## Pull requests

- Keep changes focused and scoped.
- Update `README.md` if behavior or commands change.
- Avoid committing secrets (`.env` is ignored).
